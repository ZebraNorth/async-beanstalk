name: Build
on:
    release:
        types: [created]
jobs:
    build:
        runs-on: ubuntu-slim
        steps:
            - uses: actions/checkout@v7
            - uses: actions/setup-node@v7
              with:
                node-version: '22'
            - run: npm install
            - run: npm run build
            - run: npm run docs
